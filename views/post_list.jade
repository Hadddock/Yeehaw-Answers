extends layout 

block content 
    h1=title 
    ul 
        each post in post_list 
            li 
                a(href=post.url) #{post.title}
                p #{post.body}
        else 
            li There aren't any posts!